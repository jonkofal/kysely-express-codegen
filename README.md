TODO:

API
add cli flags library and code)
--output (relative or FQP)
--databaseUrl (break it up, prompt for password?)
add publish to npmjs

EXPRESS
add post, put, delete
add pagination to gets
add getBy(JSON Object)

KYSELY
add generate types

JSON-SCHEMA
add schema generation

VALIDATION
add validation

TEST
add build target to /dist ts->js
add typescript stuff for dev
add express for dev
add test index generation
