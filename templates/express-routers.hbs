import { Router } from 'express';

{{#each this}}
import {{camelCase name}}Router from './{{camelCase name}}Router.js';
{{/each}}

const router = Router();

{{#each this}}
router.use("/{{camelCase name}}", {{camelCase name}}Router);
{{/each}}

export default router;